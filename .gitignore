.idea/

# Ruflo / Claude Code runtime
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude-flow/
.swarm/
ruvector.db
backend/cache.db-shm
backend/cache.db-wal

# AgentDB runtime state
agentdb.rvf
agentdb.rvf.lock

# Obsidian memory vault (contains a bearer token — never commit)
obsidian/bike-memory/

# Stray empty scratch file
backend/dict

# Transient SQLite WAL/SHM for the TODO-019 parity fixture
backend/tests/fixtures/*.db-shm
backend/tests/fixtures/*.db-wal
backend/tests/__pycache__/

# Agent backfill spool (transient)
docs/bikes/scratch/
# Per-run queue state (1.8MB, churns every run). rounds.json is the durable log.
docs/bikes/pipeline/state/queue.json
backend/cache.db.bak
