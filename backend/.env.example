ANTHROPIC_API_KEY=sk-ant-api03-...

# Database (TODO-028). Unset = SQLite file backend/cache.db. Local Postgres: docker container biker-pg (see README).
# DATABASE_URL=postgresql+psycopg://biker:biker@localhost:5432/biker

# Playwright photo scraping. true = no browser window (required in Docker / on a server); unset = visible browser.
# PLAYWRIGHT_HEADLESS=true

# On-demand OLX searcher (TODO-031) — the separate searcher/ service that POST /v1/bike/used/search proxies to.
# Unset SEARCHER_URL = not configured (503). SEARCHER_API_KEY must equal the searcher's own SEARCHER_API_KEY.
# SEARCHER_TIMEOUT (seconds, default 600) bounds one search.
SEARCHER_URL=http://localhost:8100
SEARCHER_API_KEY=dev-local-searcher-key
